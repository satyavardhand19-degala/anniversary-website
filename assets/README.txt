These are placeholders. Swap every one of them before you send the link.

  polaroid.jpg   lock screen photo, square works best
  hero.jpg       full-bleed photo behind the anniversary headline (landscape)
  memory-1.jpg   photo strip on the letter page (4:3)
  memory-2.jpg
  memory-3.jpg
  song.mp3       plays from the first keypad tap onward

Keep the filenames the same and you won't have to touch config.js.
If you'd rather use different names, point config.js at them instead.

song.mp3 is 30 seconds of real, valid, completely silent audio. It exists so
the page loads without a 404 in the console — it is not music. Replace it.

Compress photos before adding them: under ~300 KB each, and under 4 MB for
the audio. https://squoosh.app does the images in your browser for free.
